#include<stdio.h>
int main()
{
char senten[30];
int i,ct=1;
scanf("%s",&senten);
for(i=0;i<sentena[i]!=null;i++)
{
if(sentena[i]='@'||sentena[i]='#'||sentena[i]='$'||sentena[i]='&')
{
c++;
}
}
printf("%d",ct);
}
